Program 1 — Java + Docker + Git
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Java program, Docker image, push to GitHub & DockerHub
HelloWorld.java
public class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello from DevOps Lab!");
    }
}
Copy
Dockerfile
FROM openjdk:11
WORKDIR /app
COPY HelloWorld.java .
RUN javac HelloWorld.java
CMD ["java", "HelloWorld"]
Copy
Commands
git init
git add .
git commit -m "Java Docker app"
git remote add origin <your-repo-url>
git push origin main

docker build -t hello-java .
docker tag hello-java <dockerhub-username>/hello-java:latest
docker login
docker push <dockerhub-username>/hello-java:latest
Copy
Program 2 — C + Docker + Git
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: C program, Docker image, push to GitHub & DockerHub
hello.c
#include <stdio.h>
int main() {
    printf("Hello from DevOps Lab in C!\n");
    return 0;
}
Copy
Dockerfile
FROM gcc:latest
WORKDIR /app
COPY hello.c .
RUN gcc -o hello hello.c
CMD ["./hello"]
Copy
Commands
git init && git add . && git commit -m "C Docker app"
git remote add origin <repo-url> && git push origin main

docker build -t hello-c .
docker tag hello-c <user>/hello-c && docker push <user>/hello-c
Copy
Program 3 — HTML + Docker + Git
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: HTML page, Docker image, push to GitHub & DockerHub
index.html
<!DOCTYPE html>
<html>
<head><title>DevOps Lab</title></head>
<body>
    <h1>Hello from DevOps Lab HTML!</h1>
</body>
</html>
Copy
Dockerfile
FROM nginx:alpine
COPY index.html /usr/share/nginx/html/index.html
Copy
Commands
docker build -t html-app .
docker run -d -p 8080:80 html-app
docker tag html-app <user>/html-app && docker push <user>/html-app
Copy
Program 4 — Python + Docker + Git
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Python program, Docker image, push to GitHub & DockerHub
app.py
print("Hello from DevOps Lab in Python!")
Copy
Dockerfile
FROM python:3.10-slim
WORKDIR /app
COPY app.py .
CMD ["python", "app.py"]
Copy
Commands
docker build -t hello-python .
docker run hello-python
docker tag hello-python <user>/hello-python && docker push <user>/hello-python
Copy
Program 5 — Node + Docker + Git
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Node.js program, Docker image, push
app.js
console.log("Hello from DevOps Lab in Node.js!");
Copy
Dockerfile
FROM node:18
WORKDIR /app
COPY app.js .
CMD ["node", "app.js"]
Copy
Commands
docker build -t hello-node .
docker run hello-node
docker tag hello-node <user>/hello-node && docker push <user>/hello-node
Copy
Program 6 — Flask + Docker + Git
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Flask program, Docker image, push
app.py
from flask import Flask
app = Flask(__name__)

@app.route('/')
def home():
    return "Hello from Flask in DevOps Lab!"

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)
Copy
requirements.txt
flask
Copy
Dockerfile
FROM python:3.10-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python", "app.py"]
Copy
Commands
docker build -t flask-app .
docker run -d -p 5000:5000 flask-app
docker tag flask-app <user>/flask-app && docker push <user>/flask-app
Copy
Program 7 — C + Jenkins
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: C program, push to GitHub, Jenkins job to execute
prog.c
#include <stdio.h>
int main() {
    printf("Hello from Jenkins C Job!\n");
    return 0;
}
Copy
Jenkins Job Setup
Create a Freestyle project.
Source Code Management: Git — enter repo URL.
Build: Add build step Execute Shell:
gcc -o prog prog.c
./prog
Copy
Save and click Build Now.
Program 8 — Python Calculator + Jenkins
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Python ADD/SUB/MUL/DIV, push to GitHub, Jenkins job
calc.py
def add(a, b): return a + b
def sub(a, b): return a - b
def mul(a, b): return a * b
def div(a, b): return a / b if b != 0 else "Cannot divide by zero"

a, b = 10, 5
print(f"Add: {add(a,b)}, Sub: {sub(a,b)}, Mul: {mul(a,b)}, Div: {div(a,b)}")
Copy
Jenkins Job Setup
Freestyle project with Git SCM.
Build step Execute Shell:
python3 calc.py
Copy
Program 9 — Java Reverse + Jenkins
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Java program to reverse a number + Jenkins job
ReverseNumber.java
import java.util.Scanner;
public class ReverseNumber {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter a number: ");
        int n = sc.nextInt(), rev = 0;
        while (n != 0) {
            rev = rev * 10 + n % 10;
            n /= 10;
        }
        System.out.println("Reversed: " + rev);
    }
}
Copy
Jenkins Job
Freestyle project, Git SCM.
Build step Execute Shell:
javac ReverseNumber.java
java ReverseNumber
Copy
Program 10 — Hello-World Docker + Jenkins Periodic Build
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Docker commands to run Hello-World
docker pull hello-world
docker run hello-world
Copy
Q5: Jenkins job for shell commands, triggered periodically
Freestyle project.
Build Triggers > Build periodically: */5 * * * * (every 5 minutes).
Build > Execute Shell:
echo "Hello from Jenkins periodic build"
date
Copy
Program 11 — HTML Registration Form + Jenkins Publish
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: HTML Registration form + Jenkins publish
register.html
<!DOCTYPE html>
<html>
<head><title>Registration Form</title></head>
<body>
    <h2>Registration Form</h2>
    <form>
        Name: <input type="text"><br>
        Email: <input type="email"><br>
        <input type="submit" value="Register">
    </form>
</body>
</html>
Copy
Jenkins Job
Freestyle project, Git SCM.
Post-build Action > Publish HTML reports: set HTML directory to project workspace and index page to register.html.
Program 12 — Jenkins Pipeline Job (Jenkinsfile from SCM)
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Jenkins Pipeline using Jenkinsfile (SCM)
Jenkinsfile (in Git repo)
pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps { checkout scm }
        }
        stage('Build') {
            steps { echo 'Building...' }
        }
        stage('Test') {
            steps { echo 'Testing...' }
        }
        stage('Deploy') {
            steps { echo 'Deploying...' }
        }
    }
}
Copy
Jenkins Job Setup
New Item > Pipeline.
Pipeline > Definition: "Pipeline Script from SCM".
Select Git, enter repo URL, script path: Jenkinsfile.
Program 13 — Java & Python Pattern + Jenkins (File Param)
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Pattern programs with file parameterization
Pattern.java
public class Pattern {
    public static void main(String[] args) {
        int n = 5;
        for (int i = 1; i <= n; i++) {
            for (int j = 1; j <= i; j++)
                System.out.print("* ");
            System.out.println();
        }
    }
}
Copy
Pattern.py
n = 5
for i in range(1, n+1):
    print("* " * i)
Copy
Jenkins Job (File Parameterization)
Freestyle project, tick This project is parameterized.
Add File Parameter (name: input.txt).
Build step Execute Shell:
javac Pattern.java && java Pattern
python3 Pattern.py
Copy
Program 14 — Ubuntu Container + Jenkins Pipeline (5 stages)
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Docker — run Ubuntu container named "MyContainer"
docker run -it --name MyContainer ubuntu bash
# Inside container:
apt update && apt install -y curl
echo "Hello from Ubuntu container"
Copy
Q5: Jenkins Pipeline with 5 stages
pipeline {
    agent any
    stages {
        stage('Checkout') { steps { echo 'Checkout done' } }
        stage('Build')    { steps { echo 'Build done' } }
        stage('Test')     { steps { echo 'Test done' } }
        stage('Package')  { steps { echo 'Package done' } }
        stage('Deploy')   { steps { echo 'Deploy done' } }
    }
}
Copy
Program 15 — Python Container + k8s nginx Deployment
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Docker — run Python image and execute commands
docker run -it python:3.10-slim bash
# Inside container:
python -c "print('Hello from Python container')"
exit
Copy
Q5: Kubernetes — nginx deployment
kubectl create deployment nginx --image=nginx
kubectl get pods
kubectl expose deployment nginx --port=80 --type=NodePort
kubectl get svc
Copy
Program 16 — Node Container + k8s Python Deployment
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Docker — run Node image and execute commands
docker run -it node:18 bash
# Inside container:
node -e "console.log('Hello from Node container')"
exit
Copy
Q5: Kubernetes — Python deployment + kubectl commands
kubectl create deployment python-app --image=python:3.10-slim -- sleep infinity
kubectl get pods
kubectl exec -it <pod-name> -- python -c "print('Hello from k8s Python')"
Copy
Program 17 — nginx Container + k8s Mongo Deployment
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Docker — run nginx image and execute commands
docker run -d -p 8080:80 --name mynginx nginx
docker exec mynginx cat /etc/nginx/nginx.conf
docker stop mynginx
Copy
Q5: Kubernetes — Mongo deployment
kubectl create deployment mongo --image=mongo
kubectl get pods
kubectl logs <pod-name>
Copy
Program 18 — Containerized HTML (Registration Form) + Docker + Git
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: HTML Registration form containerised & pushed
register.html
<!DOCTYPE html>
<html>
<head><title>Registration</title></head>
<body>
    <h2>Registration Form</h2>
    <form>
        Name: <input type="text" name="name"><br>
        Email: <input type="email" name="email"><br>
        <input type="submit">
    </form>
</body>
</html>
Copy
Dockerfile
FROM nginx:alpine
COPY register.html /usr/share/nginx/html/index.html
Copy
Commands
docker build -t reg-form .
docker run -d -p 8080:80 reg-form
docker tag reg-form <user>/reg-form && docker push <user>/reg-form
git init && git add . && git commit -m "Registration form"
git remote add origin <url> && git push origin main
Copy
Program 19 — Docker Compose: busybox ping
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Docker Compose — two busybox containers, ping bbConB from bbConA
docker-compose.yml
version: '3'
services:
  bbcona:
    image: busybox
    container_name: bbConA
    command: sleep 3600
  bbconb:
    image: busybox
    container_name: bbConB
    command: sleep 3600
Copy
Commands
docker-compose up -d
docker exec bbConA ping -c 4 bbConB
docker-compose down
Copy
Program 20 — k8s nginx Deployment using YAML
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Kubernetes nginx deployment via manifest file
nginx-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
Copy
Commands
kubectl apply -f nginx-deployment.yaml
kubectl get deployments
kubectl get pods
Copy
Program 21 — k8s Node Deployment + Selenium (Open Website)
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Kubernetes Node deployment + kubectl commands
kubectl create deployment node-app --image=node:18 -- sleep infinity
kubectl get pods
kubectl exec -it <pod-name> -- node -e "console.log('Hello from Node in k8s')"
Copy
Q5: Selenium — open Google / College website
const { Builder } = require('selenium-webdriver');

(async function openSite() {
    let driver = await new Builder().forBrowser('chrome').build();
    await driver.get('https://www.google.com');
    console.log(await driver.getTitle());
    await driver.quit();
})();
Copy
Program 22 — Selenium Login Validation Test
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: JavaScript login validation + Selenium test
login.html (app under test)
<!DOCTYPE html>
<html>
<body>
    <input id="user" placeholder="Username">
    <input id="pass" type="password" placeholder="Password">
    <button onclick="alert('Login clicked')">Login</button>
</body>
</html>
Copy
Selenium test
const { Builder, By } = require('selenium-webdriver');

(async function loginTest() {
    let driver = await new Builder().forBrowser('chrome').build();
    await driver.get('file:///path/to/login.html');
    await driver.findElement(By.id('user')).sendKeys('admin');
    await driver.findElement(By.id('pass')).sendKeys('pass123');
    await driver.findElement(By.css('button')).click();
    console.log('Login test passed');
    await driver.quit();
})();
Copy
Program 23 — Selenium Exam Results Check
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Fetch exam results from college website using Selenium
const { Builder, By } = require('selenium-webdriver');

(async function checkResults() {
    let driver = await new Builder().forBrowser('chrome').build();
    await driver.get('https://matrusri.skolo.in/');
    let rollNo = await driver.findElement(By.name('rollno'));
    await rollNo.sendKeys('123456');
    let submit = await driver.findElement(By.css('input[type="submit"]'));
    await submit.click();
    console.log('Result page title:', await driver.getTitle());
    await driver.quit();
})();
Copy
Program 24 — Calculator WebApp + Selenium Test
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Web calculator + Selenium test
calc.html
<!DOCTYPE html>
<html>
<body>
    <input id="a" placeholder="Num 1">
    <input id="b" placeholder="Num 2">
    <button onclick="document.getElementById('r').innerText = Number(document.getElementById('a').value) + Number(document.getElementById('b').value)">Add</button>
    <p id="r"></p>
</body>
</html>
Copy
Selenium test
const { Builder, By } = require('selenium-webdriver');
const assert = require('assert');

(async function calcTest() {
    let driver = await new Builder().forBrowser('chrome').build();
    await driver.get('file:///path/to/calc.html');
    await driver.findElement(By.id('a')).sendKeys('10');
    await driver.findElement(By.id('b')).sendKeys('20');
    await driver.findElement(By.css('button')).click();
    let result = await driver.findElement(By.id('r')).getText();
    assert.strictEqual(result, '30');
    console.log('Calculator test passed');
    await driver.quit();
})();
Copy
Program 25 — Jenkins Remote Trigger (Java)
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Jenkins job — execute Java program from Git, trigger remotely
Sample Java (HelloWorld.java)
public class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello from remotely triggered Jenkins job!");
    }
}
Copy
Jenkins Setup
Freestyle project, Git SCM.
Build Triggers > Trigger builds remotely: set an auth token (e.g., myToken).
Build > Execute Shell:
javac HelloWorld.java && java HelloWorld
Copy
Remote trigger URL
http://jenkins-server:8080/job/JobName/build?token=myToken
Copy
Program 26 — Jenkins SCM Polling (Python Calculator)
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Jenkins job — Python Calc from Git, SCM polling (auto-execute on change)
calc.py
a, b = 15, 4
print(f"Add: {a+b}, Sub: {a-b}, Mul: {a*b}, Div: {a/b}")
Copy
Jenkins Setup
Freestyle project, Git SCM with repo URL.
Build Triggers > Poll SCM: * * * * * (every minute).
Build > Execute Shell: python3 calc.py
Program 27 — Jenkins File & Variable Param (Java & Python Calculator)
Q1: Five Git Commands and Their Usage
Command	Usage
git init	Initialises a new Git repository in the current directory.
git add <file>	Stages a file (or all files with .) for the next commit.
git commit -m "message"	Commits the staged changes with a descriptive message.
git push origin <branch>	Pushes local commits to the remote repository.
git pull origin <branch>	Fetches and merges changes from the remote branch.
Q2: Five Docker Commands and Their Usage
Command	Usage
docker build -t <tag> .	Builds a Docker image from a Dockerfile in the current directory.
docker run <image>	Runs a container from the specified image.
docker ps	Lists all running containers.
docker images	Lists all locally stored Docker images.
docker push <username>/<image>	Pushes an image to Docker Hub.
Q3: Five Kubernetes Commands and Their Usage
Command	Usage
kubectl get nodes	Lists all nodes in the cluster.
kubectl get pods	Lists all pods in the default namespace.
kubectl get deployments	Lists all deployments.
kubectl create deployment <name> --image=<img>	Creates a new deployment from an image.
kubectl expose deployment <name> --port=<port> --type=NodePort	Exposes a deployment as a service.
Q4: Java & Python Calculator + Jenkins with File & Variable Parameterization
Calculator.java
public class Calculator {
    public static void main(String[] args) {
        int a = Integer.parseInt(args[0]);
        int b = Integer.parseInt(args[1]);
        System.out.println("Sum: " + (a + b));
        System.out.println("Diff: " + (a - b));
        System.out.println("Prod: " + (a * b));
        System.out.println("Quot: " + (a / b));
    }
}
Copy
calc.py
import sys
a, b = int(sys.argv[1]), int(sys.argv[2])
print(f"Add: {a+b}, Sub: {a-b}, Mul: {a*b}, Div: {a/b}")
Copy
Jenkins Setup (File & Variable Param)
Freestyle project, tick This project is parameterized.
Add String Parameter — name: NUM1, default: 10.
Add String Parameter — name: NUM2, default: 5.
Add File Parameter — name: data.txt.
Build > Execute Shell:
javac Calculator.java && java Calculator $NUM1 $NUM2
python3 calc.py $NUM1 $NUM2
echo "File uploaded: $(cat data.txt)"
